import type { HumanAudioSample } from '../types/audio'

// Este arquivo é reescrito por `npm run audio:sync`.
// Ele permanece vazio no repositório até que a sincronização local seja executada.
export const generatedAudioSamples: HumanAudioSample[] = []
